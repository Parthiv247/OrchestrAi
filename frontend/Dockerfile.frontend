FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

RUN npm run build

EXPOSE 3001

# `next start` defaults to port 3000 — force 3001 to match the compose mapping.
CMD ["npx", "next", "start", "-p", "3001"]
