orchestrai:
  target: dev
  outputs:
    dev:
      type: postgres
      # DATABASE_URL wins in production (Railway/Supabase); fall back to individual vars for local Docker
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      user: "{{ env_var('POSTGRES_USER', 'admin') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'orchestrai_secret') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      dbname: "{{ env_var('POSTGRES_DB', 'orchestrai') }}"
      schema: public
      threads: 4
      connect_timeout: 10
